# ============================================================
# Hyojin Distribution AI Agent Team — Environment Variables
# ============================================================
# 사용법:
#   1. cp .env.example .env
#   2. 아래 ★ 표시된 4가지만 직접 입력
#   3. 나머지는 기본값 사용 또는 setup.sh가 자동 생성
#   4. .env는 절대 git에 커밋 금지 (.gitignore에 포함됨)
# ============================================================

# ------------------------------------------------------------
# OpenClaw Gateway
# ------------------------------------------------------------
# ★ 필수 — openclaw.json auth.mode: "token" 에 사용되는 게이트웨이 인증 토큰.
#   새로 발급: openssl rand -hex 32
#   이 값을 삭제하거나 비우면 게이트웨이 인증이 비활성화되어 보안 취약점 발생.
OPENCLAW_GATEWAY_TOKEN=여기에_openssl_rand_hex_32_결과값_입력

# Docker image — use a pinned digest in production for stability.
# Check latest at: https://github.com/openclaw/openclaw/releases
OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest

# ------------------------------------------------------------
# Telegram
# ------------------------------------------------------------
# ★ 필수 입력 — 텔레그램에서 @BotFather → /newbot 으로 발급
TELEGRAM_BOT_TOKEN=여기에_봇_토큰_입력

# ★ 필수 입력 — 텔레그램에서 @userinfobot 에게 아무 메시지 보내면 숫자 ID 확인
CEO_TELEGRAM_ID=여기에_대표자_텔레그램_숫자ID_입력

# ------------------------------------------------------------
# Google Gemini API
# Set monthly hard limit at https://console.cloud.google.com/billing
# This is Layer 1 budget protection (most important).
# ------------------------------------------------------------
# ★ 필수 입력 — https://aistudio.google.com → Get API key
GEMINI_API_KEY=여기에_Gemini_API_키_입력

# Pinned model versions (NEVER use "latest" — causes instability)
# Verified working as of 2026-02. gemini-2.5-flash/pro are current stable GA models.
LLM_FLASH_MODEL=google/gemini-3-flash-preview
LLM_PRO_MODEL=google/gemini-3.1-pro-preview

# Gemini 3 preview pricing (USD per 1M tokens) — update when pricing changes
LLM_FLASH_INPUT_COST_PER_1M=0.075
LLM_FLASH_OUTPUT_COST_PER_1M=0.300
LLM_PRO_INPUT_COST_PER_1M=1.25
LLM_PRO_OUTPUT_COST_PER_1M=5.00

# ------------------------------------------------------------
# Budget limits (Layer 2 — BudgetGuard enforcement)
# ------------------------------------------------------------
DAILY_BUDGET_USD=15.0
MONTHLY_BUDGET_USD=450.0
KRW_EXCHANGE_RATE=1350.0

# ------------------------------------------------------------
# PostgreSQL (BudgetGuard cost database)
# ------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=agentdb
POSTGRES_USER=agentuser
# ★ 자동 생성 — setup.sh 실행 시 자동으로 채워짐 (직접 입력 불필요)
POSTGRES_PASSWORD=replace_with_strong_password

# ------------------------------------------------------------
# Domain / SSL (for Nginx HTTPS)
# ------------------------------------------------------------
# ★ 필수 입력 — Oracle Cloud 콘솔에서 A1 인스턴스 공인 IP 확인
#   도메인이 있으면: your-domain.com
#   도메인 없으면: Oracle A1 공인 IP 주소 (예: 1.2.3.4)
DOMAIN=여기에_도메인_또는_공인IP_입력

# ============================================================
# 스킬 API 키 (Skills API Keys)
# 아래 키들을 입력하면 해당 스킬이 즉시 활성화됩니다.
# 미입력 시 해당 스킬은 동작하지 않습니다 (오류 없이 조용히 실패).
# ============================================================

# ------------------------------------------------------------
# 🔍 검색 스킬 (★★★ — 비서실장·전 에이전트 핵심 기능)
# ------------------------------------------------------------
# Tavily AI Search — 자연어 기반 AI 검색, 정확도 높음
# 무료: 월 1,000회 / 유료: $20/월 → 10만회
# 발급: https://app.tavily.com/ → 회원가입 → "API Keys"
TAVILY_API_KEY=여기에_Tavily_API_키_입력

# ------------------------------------------------------------
# 📝 할일 관리 (★★★ — 일일 9시 브리핑 핵심)
# ------------------------------------------------------------
# Todoist API — 대표자 일정·할일 목록 연동
# 무료 플랜으로 충분
# 발급: https://app.todoist.com/app/settings/integrations/developer → "Create token"
TODOIST_API_KEY=여기에_Todoist_API_키_입력

# ------------------------------------------------------------
# 📺 유튜브 데이터 (마케터·기획PM)
# ------------------------------------------------------------
# YouTube Data API v3 — 경쟁사 채널 분석, 트렌드 영상 조사
# Google Cloud Console에서 무료 발급 (일 10,000 유닛 무료)
# 발급: https://console.cloud.google.com/ → API 및 서비스 → YouTube Data API v3 → 사용 설정 → 사용자 인증 정보 → API 키
YOUTUBE_API_KEY=여기에_YouTube_API_키_입력

# ------------------------------------------------------------
# 📋 문서 관리 (기획PM·재무담당·CS담당)
# ------------------------------------------------------------
# Notion Integration Token — 회사 문서·DB 연동
# 무료 (Notion 계정 있으면 즉시)
# 발급: https://www.notion.so/my-integrations → "New integration" → 이름 입력 → 저장 → 토큰 복사
NOTION_API_KEY=여기에_Notion_API_키_입력

# GitHub Personal Access Token — 코드 저장소 연동
# 무료
# 발급: https://github.com/settings/tokens → "Generate new token (classic)" → repo 권한 체크
GITHUB_TOKEN=여기에_GitHub_토큰_입력

# ============================================================
# 📈 자산관리사 API 키 (Asset Manager Market Data APIs)
# 코인·주식·경제지표 분석에 사용. 미입력 시 웹서치 스킬로 대체 동작.
# ============================================================

# ------------------------------------------------------------
# 📊 거시경제 — FRED (미국 연방준비제도 공식 API)
# 용도: 미국 기준금리·CPI·M2 통화량·실업률 등 연준 경제지표
#       코인·주식은 연준 금리 정책에 가장 민감 → 투자 판단 필수 데이터
# 무료: 무제한 호출 (가장 먼저 발급 권장)
# 발급: https://fred.stlouisfed.org/docs/api/api_key.html
#        → 이름·이메일·용도 입력 → 이메일 인증 → 즉시 발급
# ------------------------------------------------------------
FRED_API_KEY=여기에_FRED_API_키_입력

# ------------------------------------------------------------
# 🪙 코인 시세 — CoinMarketCap
# 용도: 비트코인·알트코인 실시간 시세, 시가총액, 24h 등락률, 신규 상장 코인
#       CoinGecko 대비 한국 투자자 인지도 높음 / 거래소 데이터 교차 검증용
# 무료: 월 10,000 크레딧 (일반 호출 1크레딧 → 충분)
# 발급: https://coinmarketcap.com/api/ → "Get Your Free API Key"
#        → 이메일 입력 → 가입 → 대시보드에서 키 확인
# ------------------------------------------------------------
COINMARKETCAP_API_KEY=여기에_CoinMarketCap_API_키_입력

# ------------------------------------------------------------
# 🇰🇷 한국 주식 — KRX Open API (한국거래소 공식)
# 용도: 코스피·코스닥 전 종목 시세, 지수, 거래량, 배당 정보
#       대표자 한국 주식 매매 분석용. Alpha Vantage는 국내 중소형주 미지원.
# 무료: 무제한 (회원가입 후 발급)
# 발급: https://openapi.krx.co.kr → 회원가입 → 마이페이지 → Open API 신청
#        → 승인 후 키 발급 (1~2 영업일 소요)
# ------------------------------------------------------------
KRX_API_KEY=여기에_KRX_API_키_입력

# ------------------------------------------------------------
# 📉 주식·환율·경제지표 — Alpha Vantage
# 용도: 미국 주식 (나스닥·S&P500) OHLCV 데이터, RSI·MACD 기술지표 자동 계산,
#       USD/KRW 환율 (코인 원화 환산 필수), 미국 GDP·CPI 거시지표
# 무료: 일 25회 (분석용으로 충분)
# 발급: https://www.alphavantage.co/support/#api-key
#        → 이름·이메일 입력 → 즉시 발급 (가입 불필요)
# ------------------------------------------------------------
ALPHA_VANTAGE_API_KEY=여기에_AlphaVantage_API_키_입력

# ------------------------------------------------------------
# 😨 코인 시장 심리 — Fear & Greed Index
# 용도: 코인 시장 공포/탐욕 지수 (0=극도 공포, 100=극도 탐욕)
#       매수·매도 타이밍 판단 보조 / 극도 공포 구간 = 역사적 매수 기회
# API 키 없음: https://api.alternative.me/fng/ 무료·무제한 직접 호출
# 별도 입력 불필요 — 자산관리사가 URL 직접 호출함
# ------------------------------------------------------------
# (키 없음 — 자산관리사 SOUL.md에 엔드포인트 URL 직접 명시)

# ------------------------------------------------------------
# 🔄 거래소 연동 — Bybit (Phase 2 — 추후 대표자 제공)
# 용도: 코인 잔고 조회·매수·매도·손익 보고 자동화
#       현재: API 미연동 상태. 분석·자문만 제공.
#       준비되면: Bybit 계정 → API 관리 → 키 생성 (읽기 + 거래 권한) → 아래 입력
# 발급 (나중에): https://www.bybit.com/app/user/api-management
# ------------------------------------------------------------
# BYBIT_API_KEY=여기에_Bybit_API_키_입력
# BYBIT_SECRET=여기에_Bybit_시크릿_키_입력
